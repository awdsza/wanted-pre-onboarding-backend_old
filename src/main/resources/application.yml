
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/onboarding
    username:
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
       format_sql: true
logging:
  level:
    # 로거를 통해찍음.
    org.hibernate.SQL: debug
    org.hibernate.orm.jdbc.bind: trace
